# Attention Is All You Need

The Transformer architecture is introduced in a paper that relies only on attention mechanisms, without recurrent or convolutional networks.

## Summary

The model uses multi-head self-attention, encoder-decoder blocks, and positional encodings to handle sequence transduction tasks.
