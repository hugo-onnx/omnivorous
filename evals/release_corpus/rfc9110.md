# RFC 9110 HTTP Semantics

RFC 9110 defines HTTP semantics, including request methods, response status codes, representation metadata, and general protocol terminology.

## Terminology

It describes concepts such as resources, representations, content negotiation, and semantics shared across HTTP versions.
